# RunPod API token is used only by external provisioning tools/CLI.
# Do not put it in the notebook runtime and never commit the real .env file.
RUNPOD=replace-with-your-runpod-api-token

# Optional runtime overrides for the notebook.
# WS=/workspace/steering-role-confusion
# DEVICE=cuda:0
